<nav class="navbar">
    <a class="navbar__logo" href="{{ path ('home') }}">Sortir </a>
    <ul class="navbar__list">
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <li><a href="{{ path ('home') }}">Accueil <i class="fas fa-home"></i></a></li>
            <li><a href="{{ path ('create_event') }}">Créer une sortie <i class="fas fa-plus-circle"></i></a></li>
            <li><a href="{{ path ('profile', {id: app.user.id }) }}">Mon profil <i class="fas fa-user"></i></a></li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="admin"><a href="{{ path('user_admin') }}">Admin <i class="fas fa-users-cog"></i></a></li>
            {% endif %}
            <li><a href="{{ path ('app_logout') }}"> Se déconnecter <i class="fas fa-sign-out-alt"></i></a></li>
        {% else %}
            <li><a href="{{ path ('app_login') }}">Se connecter</a></li>
        {% endif %}

    </ul>
</nav>

